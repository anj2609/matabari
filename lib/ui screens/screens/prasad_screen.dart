import 'package:flutter/material.dart';

class PrasadScreen extends StatelessWidget {
  const PrasadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Prasad Screen")),
    );
  }
}